-- 设置主题
require("onedark").setup({
    style = "dark",
    transparent = false,
    term_colors = true,
    ending_tildes = false,
})

require("onedark").load()

-- UI 相关配置（插件配置已迁移到 lazy.nvim）
-- 此文件仅包含与UI相关的全局设置
